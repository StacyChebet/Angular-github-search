export class Repo {
  // tslint:disable-next-line:max-line-length
  // constructor(public id: number, public name: string, public description: string, public homepage: string, public forks: number, public fork: boolean, public notLive: boolean)
}
